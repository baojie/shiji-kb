# 反思 2026-04-22 (第六次 · v6)

> 触发: 自 v5 (#190) 累计 ~70 条 actions 到 #260.
> 规模: wiki 166 → 204 页 (+23%), recent 500+493 → 500+530 归档 (+37).

---

## 一、核心指标

- 70+ actions, 0 fail (canonical 过滤 + 手工筛联合拦截)
- user-req 集中爆发: #5 (渲染清理) · #6 (history JSON 内联) · #7 (归档翻页) · #8 (diff 视图)
- W5 自触发反思在本轮**未主导改动**, 都是 user-req 驱动
- create-stub 占比回到 ~70% (user-req-4 深度周期结束后回归广度)

## 二、前次 v5 提案验证

### 提案 14 (archive 触发日志) — ⚙️ 未实施
忘了做. 补进 v6 待办.

### 提案 15 (扩展 stub 清理) — ✅
14 页 + 13 页 清 stub 标记, 响应 timeline / cite / 3 tags.

### 提案 16 (link-external-docs) — ✅ 带修正
151 页加 docs/entities 链, 但 topic 页被误加后回滚 2 页.
下次应类型感知.

## 三、两个重大架构演进 (user-req 驱动)

### 架构 A · history 存储 (user-req-6)

1159 小文件 → 166 大 JSON. 体积 -72%. 前端 fetch 路径变更.
教训: 初版选择"每 rev 一文件"是过早最佳化 (对齐 Git 思路), 实际 wiki 场景不需要.
**下次启动新存储**: 先想"有多少实体", 文件粒度匹配.

### 架构 B · 归档 + 翻页 + diff (user-req-7, -8)

recent.json 不再 500 截断, 而是**软上限 + 溢出归档**. 翻页穿透归档.
diff 是纯 LCS 行级, 无 CDN. 等宽字体, 红绿高亮.
下次**新功能**模式模板: 前端新路由 → renderer 函数 → router 接 → CSS.

## 四、新识别模式

### 模式 U · W5 与 user-req 分治

本轮 user-req 占 4 (连编号 5-8), W5 反思 0. Butler **越来越反应式**, 用户反馈驱动, 主动反思沉默。

**假设**: 当前 butler 产出已满足基本需求, 用户关注"质量"和"能力"。反思应转向主动识别**质量漏洞**, 不只是执行层优化。

**提案 17**: W5 v7+ 反思应包含:
- 扫 **alias_conflicts.json**: 选 3 个最严重的冲突, 提案消歧方案
- 扫 **无 timeline 的 18 页**: 为什么没覆盖? KG 事件索引盲区?
- 扫 **duplicate_candidates.json**: 自动扩 CANONICAL_MERGE

### 模式 V · create-stub 仍是主力 (70%)

尽管 user-req-4 深度优先规则存在 (median 低时触发), median 已 26 常驻高位, 规则永不激活。
但新加实体都是从 stub 起步, 所以稳态下 stub 比例偏高。

**假设**: 深度/广度规则过于粗粒度, 应按**每页**单独判定。

**提案 18**: 规则精细化:
- 页面级: 若 quality_score < 10 且存在丰富源 → 优先触发该页 enrich, 而非全站策略
- 实现: W2 新动作 `enrich-by-page` 先做单页梯度

### 模式 W · 无事件页长期低分

18 页 (诸侯/侯者等) 无 timeline, quality 停在 10-12。KG 事件粒度不到位是根本, butler 能做的有限。

**提案 19**: 不强求这些页达标。在 `_score_parts` 加 `has_events_data` 标志, rubric 对无事件数据的页放宽要求 (不减 rev/size 权重, 但免扣 narrative 分)。

## 五、修订清单 (≤3, W5 保守)

1. **提案 17** · W5 反思扫描结构化列表: alias_conflicts / duplicate_candidates / no-timeline pages
2. **提案 18** · 页面级 enrich 优先: 若单页 q < 10 且有丰富源 → 立即处理
3. **提案 19** · 对无事件数据页放宽 rubric (知识图谱空白非 butler 之过)

## 六、待清理的小债

- `归档触发日志` (v4 提案 14 漏实施)
- topic 页的 link-external-docs 应基于 type 智能选链
- discover_kg 过滤规则列表长到 10+ 行, 可提取成 JSON 配置

## 七、数据观察累积

- 204 页中 2 topic + 202 person, 比例 99:1
- 其他实体类型 (state/place/event/chapter) 数量仍 0
- 下一波食物源扩展必须啃这些 — butler v0.x → v1.0 的升级方向
