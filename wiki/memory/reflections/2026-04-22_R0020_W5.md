# 反思 2026-04-22 (第三次 · v3)

> 触发：自第二次反思 (v2) 后累计 47 条 actions.
> 素材：actions #41-88 + recent.json + quality distribution.

---

## 一、素材汇总

- 47 条记录，**0 失败**（!）
- action 分布：
  - create-stub **29** (62%)
  - add-tag-batch 5 (11%)
  - add-tag 3
  - maintenance 5 (quality_score / all-page / pagination / rebuild / backfill)
  - reflect-apply 4 (提案 4-7 应用)
  - reflect 1 (自身)
- mode 分布：trail 37 / maintenance 5 / reflect-apply 4 / reflect 1
- wiki 规模：44 → **59** 页 (+34%)

## 二、前次提案验证

### 提案 5 · canonical 重复检测 — 🟡 部分
- 输出 1564 对候选, 绝大多是"武子/昭公"类谥号噪音
- 真正价值的仅个位数 (秦二世/二世皇帝在内但没显式发现)
- **需下次迭代**: 过滤谥号模式, 按共享强别名的"独特程度"排序

### 提案 6 · 标签词表 — ✅ 可用
- discover_tags 30+ 页有新建议
- threshold 0.40 后"本纪人物"误推减少
- era 仍按 birth_ce 判断, 刘邦被误标"战国" (应按 death_ce)
- **需下次迭代**: era 改用 death_ce 或中点

### 提案 7 · 动态配比 — ⚙️ 未验证
- 本批 P1 队列常年 > 10, 规则要求 3:1
- 实际 trail 37 / explore ~0 (超偏 trail)
- 原因: 队列一直充足, explore 没机会触发
- **不急修**, 等队列缩小后自然对照

## 三、用户需求三项 (非反思驱动, 实时紧急)

- #1 分页 recent.json 不再 50 截断 ✅
- #2 quality_score 多因子 ✅
- #3 All 页独立 ✅

这些**绕过了 W5 流程**, 直接作为"用户反馈 → 立即应用". W5 协议需要 vote 这个做法:
- ✅ 保留: 紧急反馈不该等 20 条 action 再反思
- 可选: 在 skill_changes.md 标记 `user-req` 与 `w5-reflect` 区分

## 四、新模式识别

### 模式 K · create-stub 占比再次走高 (62%)

尽管 v2 提案 6 引入 add-tag 多样化, v3 阶段 create-stub 又回到主导。

**假设**: 食物源 A (kg) 顶端候选太多, trail mode 下优先消费. W1 v0.3 的"源权重衰减"未触发 (A 源从未空过)。

**不修**. 客观上需要这么多 stub; v4 以后可重新评估。

### 模式 L · quality_score 分布扁平

59 页中 37 页在 [10-15) 区间, 中位数 14. 只有 刘邦 (44) 和 项羽 (36) 超 30.

**假设**: rev_count 对大多数页 = 2 (create + tag), size 全 ≤ 500 B (新 stub 模板). 多因子被 base_refs 支配。

**提案 8**: 增加 **narrative_bonus**:
- body 含 `<!-- stub:` → -5 分 (惩罚纯 stub)
- body 除表格外有 ≥ 2 段散文 → +8 分 (鼓励人工叙述)

这样 stub 和有内容的页能明确区分。

### 模式 M · 用户需求绕过 W5 流程

butler 应该**区分**:
- W5 周期反思驱动的提案 (记 `[reflections/<date>.md]`)
- 用户紧急反馈 (记 `[user-req-<N>]`)

两者都写 skill_changes.md, 但不同前缀便于溯源.

**提案 9**: skill_changes.md 格式规范化:
```
YYYY-MM-DD  [w5-v<N> | user-req-<N>]  <文件/skill>  <摘要>
```

## 五、修订清单 (≤3)

### 提案 8 · narrative_bonus

修 `wiki/scripts/build_registry.py` 的 quality_score 计算:
- 检查 body 里 `<!-- stub:` 注释 → -5
- 检查非表格非列表的散文段落数 ≥ 2 → +8

**预期**: 手写 topic 页 (如 司马迁的史学思想) 分数上 50+, stub 保持 10-15.

### 提案 9 · skill_changes.md 分类前缀

改已有记录 + 定义新规则. 一次性清理.

### 提案 10 · discover_tags era 改用 death_ce

修 `wiki/scripts/butler/discover_tags.py`:
- era_tag() 签名改收 `(birth_ce, death_ce)`
- 优先 death_ce 落区间, fallback birth_ce
- 目的: 刘邦 (前256 生, 前195 卒) 改到"汉"而非"战国"

## 六、数据层发现 (不归 butler 处理)

新发现或持续观察:
- 姒句践 (越王勾践, 姓姒) - "姒"前缀正常, 不是 bad canonical, 但 prefix 填法不一致
- 范雎 aliases 6 个但 refs 只 2 — CANONICAL_MERGE 已合并 范睢, 但 alias 行 refs 仍多归原 canonical
