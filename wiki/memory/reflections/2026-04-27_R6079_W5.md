# 反思 2026-04-27（round=6079）

## 素材
- actions R5000–R6079（最近 50 条全部为 H23-地名地图）
- failures.jsonl 共 3 条（R4964 前：create-stub 2次，dedup-merge 1次）
- 上次反思：2026-04-25 R4964 v9（提案P26/P27/P28：update-wanted-pages强制/verify-citations/规模临界）
- check_scale.py 快照（本轮运行）
- 对话日志：2024-04-26~27

## 健康指标快照

| 指标 | R4964值 | R6079值 | 变化 | 状态 |
|------|---------|---------|------|------|
| pages.json | 2516 KB | 5516 KB | +119% | 🔴 临界 |
| wiki 总页面数 | 9394 | 20418 | +117% | 🔴 临界 |
| person 页数量 | 1273 | 4878 | +283% | 🔴 临界 |
| history/ 文件数 | 9392 | 20234 | +115% | 🔴 临界 |
| accept 率（近50条） | 41.8% | 100% | ↑ | ✅ |
| H23 连续轮次 | — | 40+ | 无限 | 🟡 多样性违规 |

## 模式识别

### 模式 A：H23 类型多样性约束未执行（严重）

**现象**：R5000–R6079 总计约 1080 轮，其中 R6040 至今全部为 H23-地名地图。W0 §六"类型多样性约束"规定"某类型在最近 6 条中连续出现 ≥3 次 → 进入冷却"，但该规则完全未被执行。

**根因**：H23 是批量模式，由用户显式启动，每轮产出 3-4 个页面。butler 在批量任务下优先执行指定类型，W0 的多样性约束被隐式绕过。

**影响**：
1. queue.md 中的 P1 expand-content、premium-upgrade 候选积压未处理
2. housekeeping_queue.md 的 194 条 P0/P1 任务未被消化
3. 曹参征战时间线（P0）长期未执行

**已有规则**：W0 §六 H23-manual 节流（每5轮≤1次），但 H23（非 manual）无节流。

### 模式 B：H23 违反 wiki 页面写入铁律

**现象**：R6072–R6079 的 H23 实现使用直接 Edit 工具修改 wiki/public/pages/*.md，并直接调用 record_revision.py，而非使用 edit_page.py 脚本。

**违反规则**：W0 §禁止清单：
- ❌ 直接 Edit/Write `wiki/public/pages/*.md`
- ❌ 直接调用 `record_revision.py`

**根因**：H23 流程需要（1）修改 frontmatter 字段、（2）添加 `## 地理位置` 节。edit_page.py 接受完整页面内容文件并覆写，可以实现相同效果，只需在调用前生成 temp_file。

**修复路径**：H23 应改为：读取当前页面 → 在内存中修改 frontmatter + 插入 `## 地理位置` → 写入 /tmp/\<slug\>_patch.md → 调用 `edit_page.py <slug> /tmp/<slug>_patch.md`。

### 模式 D：规模临界继续恶化（已知）

pages.json 从 R4964 的 2516KB 增至 5516KB，总页面数从 9394 增至 20418。R4964 v9 已写 arch_escalation 提案并等待用户批准。本次不重复写架构提案。

**状态跟踪**：arch_2026-04-25_escalation.md 存在，仍为 pending。

### 模式 E：queue.md 候选池耗尽

queue.md 仅剩 1 条 P0（曹参征战时间线），P1 条目多已标 [x]。需要补充。

### 模式 G：随机质量抽查

抽样 10 页：大陆、漳南、钜鹿、广川、鄗、胘雷、巫、西方、齐赵、洌口

| 检查项 | 通过数 | 说明 |
|--------|--------|------|
| Q1 史记引文节 | 10/10 | ✅ 全部有 |
| Q4 frontmatter完整 | 10/10 | ✅ 全部有 |
| Q7 wikilink ≥1 | 10/10 | ✅ 全部有 |
| 图片配图 | 5/10 | H23 覆盖了大陆/漳南/钜鹿/广川/鄗 |

无系统性质量问题，H23 产出质量良好。

## 修订清单（≤3 条 skill 修改）

### P29：H23 写入改为 edit_page.py 工作流

**修改目标**：SKILL_W10_Butler内务整理.md（H23 节）

**内容**：
> H23 写入 wiki 页面时，必须通过 edit_page.py，不得直接 Edit。
> 工作流：读取页面 → Python 在内存中修改 frontmatter + 插入 `## 地理位置` →
> 写入 /tmp/<slug>_h23.md → 执行 `python3 wiki/scripts/butler/edit_page.py <slug> /tmp/<slug>_h23.md --summary "map: 补写谭图配图" --author map`
> place_index.json 仍可直接写（非 wiki 页面）。

### P30：H23 多样性节流规则

**修改目标**：SKILL_W10_Butler内务整理.md（H23 节末）

**内容**：
> H23 连续执行不得超过 3 轮（含）。第 4 轮起强制插入 1 轮其他类型任务
> （从 housekeeping_queue 或 queue.md 取优先级最高的非 H23 条目），
> 之后可继续 H23。计数方法：查 actions.jsonl 最近 4 条是否全为 H23-地名地图。

## R 组队列补充（不计入修订限额）

将以下条目加入 queue.md（expand-content 候选，来自 pages.json 高 refs 过滤）

## 已排除

- failures.jsonl 3 条均为 R4964 前，与当前 H23 批量无关
- H23 连续 40+ 轮 accept 率 100%，行动质量本身无问题
- Pattern F（编辑错误扫描）：对话日志未见用户报告数据丢失事件
