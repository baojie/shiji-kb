# W5 反思 — R6625 (2026-04-27)

> 上次 W5：R6080（2026-04-27早），本次 gap = 545 轮（远超50轮警戒线，属强制补做）

---

## 一、模式识别

### A. 失败聚集
- failures.jsonl 仅有 3 条（全部来自 R6080 之前），近 545 轮内无新失败
- accept 率接近 100% → 当前批量模式风险极低

### B. 主要工作类型分布（R6080–R6625）
| 动作类型 | 次数 |
|---|---|
| expand / expand-content | 361（合计） |
| redirect | 84 |
| H23-地名地图 | 61 |
| wiki-commit | 27 |
| disambiguation | 1 |

- **expand-content 批量接近完成**：standard person 页面中，所有拥有 2+ 非薄源的页面均已获得 `## 生平` 节（共 429 页有生平，607 页无生平但均无实质源）
- **redirect 批量接近完成**：已处理约 84 个 standard→featured/premium REDIRECT
- **H23 地名地图**：大量地名页已添加地图，占据主要工作份额

### C. 阈值与优先级

- expand-content 策略已饱和：剩余 607 个无生平 person 页均只有薄源（年表），无法扩展
- redirect 策略也近饱和：alias_index 中标准→精品的 REDIRECT 机会已大量处理
- 下一阶段需转向其他工作类型

### D. 规模检查（临界）
- pages.json: 5387 KB（临界≥1024）
- 总页面数: 20418（临界≥1500）
- person 页: 4772（临界≥1000）
- history 文件: 20237（临界≥2000）

→ **规模全面超临界，需要架构提案**（见下方）

### E. 队列健康
- queue.md P1 候选：部分任务已完成（如殷契/秦惠公为file-slug错配，赵敬肃王/赵国赵桓子仅薄源）
- 真正待办 P1 剩余：西魏王（disambiguation或expand？）、楚王（disambiguation？）、王（disambiguation）
- housekeeping_queue：大量 H1 假阳性已处理

### F. 系统性编辑错误
- 无新的内容丢失报告
- edit_page.py 正常工作，hash 去重防止重复写入

### G. 随机质量抽查
- 最近写入的传记页（赵兼、陈共公、姬喜等）内容结构良好，原文引用完整
- REDIRECT 页格式统一，符合规范

---

## 二、提案

### 提案 1：扩展下一阶段工作策略到 basic→standard 晋升

**观察**：basic 质量页有 13200 个，其中 person 类型占大量，很多仅有"出现于X处"的骨架内容。部分 basic 页有实质内容，可以升级到 standard。

**提案**：在 W1 食物收集中，增加"basic→standard 晋升"扫描：查找 basic 质量 person 页面，且该页面有 1+ 非薄源，页面长度 < 1000 字符，尝试写入基础 `## 生平` 节。

**修订文件**：`SKILL_W1_Butler探索与食物收集.md` — 在 P2 后增加 "basic-升standard" 候选类型。

### 提案 2：H23 地图工作已大量完成，降低其优先级

**观察**：H23 地名地图占最近 61 次，比例仍高。若主要地名已覆盖，继续 H23 边际效益递减。

**提案**：在 housekeeping_queue.md 中标记 H23 为 P2，仅当其他 P1 队列耗尽时执行。

**修订文件**：`SKILL_W10_Butler内务整理.md` — 调低 H23 优先级说明。

### 提案 3：启动 standard person → standard person 的 REDIRECT（同级去重）

**观察**：存在明显的同人异名情况（如 姬喜 vs 燕王喜 两个 standard 页面）。current 协议只允许 standard→featured/premium REDIRECT。

**提案**：对于同人异名的两个 standard person 页面，允许将内容较少（<2000 字）的那个改为 REDIRECT，指向内容较多的那个（>3000 字）——前提是其中一个是该人的通称。

**修订文件**：`SKILL_W2_Butler原子行动目录.md` — 在 redirect 动作下增加"standard→standard 去重允许条件"。

---

## 三、架构提案（规模临界）

**当前状态**：
- 20418 页，其中 basic 13200 个（占 65%）
- pages.json 5.3MB，history/ 目录 20237 个文件

**分析**：
- basic 页面大多数是"出现于N处"的骨架，不构成真正的知识内容
- history/ 文件数量最令人担忧，每次 edit 都创建一个 JSON 文件
- 近期工作（REDIRECT/expand-content）不断增加页面数，无法减少

**提案（架构级）**：
1. **不新增 basic 页面**：仅升级已有 basic 页面，不再批量创建 basic 质量骨架页
2. **history/ 归档机制**：超过 180 天未编辑的页面，其 history JSON 可压缩归档
3. **pages.json 按需加载**：考虑将 pages.json 拆分为 index（轻量）+ detail（按需）

> ⚠️ 架构提案需要用户 review 才能执行。W5 仅记录，不执行。

---

## 四、本次修订执行（≤3 条）

提案 2（H23 降优先级）风险低，本轮执行。
提案 1 和 3 留存，待下次 W5 或用户确认。

**已执行**：在 housekeeping_queue.md P1 任务中为 H23 相关条目添加 P2 标注（写入 actions.jsonl）。

---

*触发轮次：R6625 | gap=545 | 作者：butler-w5*
