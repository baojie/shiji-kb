# 反思 2026-04-28（round=9959）

## 素材
- actions 最近 50 条（R9910–R9959）：全为 add-tag 类型，100% accept
- failures.jsonl：共 3 条（来自较早期轮次）
- 上次 W5：R6823（2026-04-27），gap = 3136 轮（远超 50 强制触发）
- 规模检查：check_scale.py 输出（4项🔴临界）

## 识别的模式

### 模式 A: 失败聚集
- 无最近 fail（最近 50 轮全 accept）
- failures.jsonl 共 3 条，来自早期，已处理或过期

### 模式 B: 成功未被规则化
- add-tag 批量操作：R9753–R9959 共 207 轮，100% accept
- 已完成 standard 品质 person/deity 页面 ≥3 标签达标
- 模式规范：按 sources 字段对应的史书来源推断时代/身份标签是高效且准确的方法

### 模式 C: 阈值失调
- 无异常

### 模式 D: 规模失调（🔴 临界，需架构提案）
- pages.json: 5.4 MB（临界线 1 MB，已超 5x）
- 总页面数: 20408（临界线 1500，已超 13x）
- person 页数: 4753（临界线 1000，已超 4x）
- history/ 文件数: 20227（临界线 2000，已超 10x）
→ 需输出 arch_2026-04-28.md 架构提案

### 模式 E: 队列候选健康
- queue.md 中仍有 premium-upgrade 候选（周武王、二世皇帝等 15+ 条）
- expand-content 候选池：1116 页，但 queue.md 中的旧条目多已完成
- 需补充新的 expand-content 候选（refs≥12 的 standard/basic 人物页）

### 模式 F: 编辑错误扫描
- 本次反思间隔过长（3136 轮），无法精确扫描对话 JSONL
- 上次 W5（R6823）已记录 Q3 检查误判问题，本次继续确认

### 模式 G: 随机质量抽查（10页）

抽样页面：（淮南王，谋反自杀，无谥）、（广陵王，祝诅自杀，无谥）、齐文公、齐成子、齐懿王、尉、孟武伯、公子围、将种、大几

| # | 检查项 | 结果 |
|---|--------|------|
| Q1 | 史记引文节存在 | 9/10（公子围是 REDIRECT，合理）|
| Q2 | 声称次数与实际条数匹配 | 未严格核验 |
| Q3 | 引文格式 `> **出自` | **0/10 — 系统误判！实际格式为 `> 出自`（无粗体）** |
| Q4 | frontmatter 字段完整 | 10/10 |
| Q5 | PN 引文对应 | 未核验（多数为自动生成引文，PN 字段未填）|
| Q6 | 正文不为空 | 9/10（将种、大几为 stub）|
| Q7 | wikilink ≥ 1 | 10/10 |

**Q3 系统性问题（第 2 次确认，≥2 次）**：
- 实际引文格式为 `> 出自 [[...]]（...）：正文`（无粗体 `**`）
- W5 Q3 检查方法 `grep '^> \*\*出自'` 只能检测粗体格式（4983 条）
- 绝大多数引文（24385 条）使用非粗体格式 `> 出自`，均被误判为 Q3 不达标
- **结论**：Q3 检查失效，需修正为接受两种格式

## 质量规则沉淀（Pattern G ≥2 次同类问题）

新增 `wiki/logs/butler/quality_rules.md` 条目：
```
- [R9959 确认，R6823 首次发现] 引文格式存在两种形式：`> 出自`（主流）和 `> **出自`（粗体少数）。Q3 检查必须同时接受两种格式，使用 `grep '^> \*\{0,2\}出自'` 或正则 `^> \*{0,2}出自`。
```

## 修订清单（skill 修改，≤3 条）

1. **W5 §二 Pattern G Q3 检查方法修正**：
   - 当前：`grep '^> \*\*出自'`
   - 修正为：`grep -E '^> (\*\*)?出自'`（同时接受粗体和非粗体）

2. **queue.md 补充 expand-content 候选**（≥30 条，refs≥12，无生平节）：
   - 卫顷侯、城阳共王、卫伉、文侯申、曹奇、秦惠公、蔡戴侯、蔡灵侯、赵敬肃王、霍嬗
   - 东周惠王、公孙衍、吴广、姬喜、江都易王、济北贞王、赵国赵桓子、韩懿侯、齐康公
   - 东周哀王、楚子西、燕惠侯（共 22+ 条）
   （不计入修订限额，是观察补充）

3. **此次大跨度 add-tag 运动规则化**（写入 kb/w5_ops.md）：
   - 成功模式：批量 add-tag 操作通过 `sources` 字段推断时代/身份标签，accept 率 100%
   - 可规则化为：每次 W5 后检查 standard 品质 person/deity 页面 tag 数量，不足 3 个的纳入 housekeeping_queue（H20-tag-补充）

## R 组队列补充（不计入修订限额）
- 无需 repair 类条目

## 已排除
- Q6 将种/大几 stub 状态：这些是 basic/standard quality stub，正常现象，不作修复
- 公子围 无引文节：该页为 REDIRECT，格式正确

## expand-content 候选补充（不计入修订限额）

见下方 queue.md 追加部分。
