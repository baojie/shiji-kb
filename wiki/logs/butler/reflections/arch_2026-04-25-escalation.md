# 架构提案 2026-04-25（升级版）

> 原始提案：`arch_2026-04-23.md`（写于 pages=671 时）
> 本文为升级补充，反映当前实际规模。

## 触发指标（当前快照，2026-04-25）

| 指标 | 写原提案时（2026-04-23） | 当前值 | 变化 |
|------|------------------------|--------|------|
| wiki 总页面数 | ~671 | **9394** | +14× |
| pages.json 大小 | ~329 KB | **2516 KB** | +7.6× |
| person 页数量 | ~200 | **1273** | +6× |
| history/ 文件数 | ~1000 | **9392** | +9× |

## 现状说明

根据 W0 §四.D 架构阈值表的数量级心智模型：

> - N < 500：纯静态 JSON + 客户端渲染，无问题  
> - 500–1500：需要**分页**与**懒加载**  
> - **1500–5000：需要预建索引（lunr.js / 静态搜索）**  
> - **5000+：需要后端查询层（FastAPI + SQLite / DuckDB），静态站点不再适合** ← 当前区间

当前 9394 页已进入 **5000+ 的后端查询层区间**。原提案 A（首字过滤）和 B（lunr.js 索引）仍有价值，但已不足以解决根本问题。

## 更新后的方案推荐

### 方案 A（已在原提案，仍推荐）
首字过滤栏 + 类型折叠 + 懒加载列表  
→ 解决前端渲染卡顿，可立即实施，工作量 ~4h

### 方案 B（新增优先级）
构建时生成 `search_index.json`（lunr.js）  
→ 9000+ 页的全文搜索必须预建索引，否则无法实用  
→ 工作量 ~8h

### 方案 C（升级为最优选）
引入本地 SQLite + FastAPI 查询层  
→ 在 N>5000 的规模下，静态 JSON 已成为性能瓶颈  
→ 工作量 ~2天  
→ **建议在下次较大版本迭代时同步引入**

## 建议执行顺序

1. **本周**：方案 A（前端过滤 + 懒加载），解燃眉之急
2. **近期**：方案 B（lunr.js 搜索索引），提升可用性
3. **下一阶段**：方案 C（SQLite 后端），根本性解决

## 等待用户批准

Butler 不会自动执行以上任何方案。  
请用户回复"批准方案 A/B/C"或"暂不处理"后，Butler 再行动。

原提案队列条目（queue.md P0 ARCH）仍有效，本文补充数字更新。
