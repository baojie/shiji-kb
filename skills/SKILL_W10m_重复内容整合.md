---
name: SKILL_W10m_重复内容整合
title: Wiki 内务整理 H12：节级重复内容整合
description: 处理不同页面间节级别的内容重复（同一引文段落出现在两个页面，或两个概念页描述同一事物的不同方面），通过删除冗余、合并互补、加交叉链接来整合。
---

# SKILL W10m: 重复内容整合（H12）

> "整页重复是 H1 的任务，节级重复是 H12 的任务。区别在粒度，不在原则：宁愿保留，不得删除有价值的内容。"

---

## 一、何时执行

| 触发场景 | 优先级 |
|---|---|
| 同一段引文出现在两个不同页面 | P1 |
| 两个概念页描述同一事物的不同方面（可互补） | P1 |
| W9 扫描发现跨页重复节 | P1 |

**与 H1 的区分**：
- H1（W10a）：整页重复（Jaccard 相似度 > 0.4）→ 整页去重合并
- H12（本文）：节级重复（特定 `##` 节内容重复）→ 节内整合

---

## 二、发现候选（扫描方法）

```bash
# 查找跨页重复的引文（长度 > 20 字）
python3 -c "
import glob, re
quotes = {}
for f in glob.glob('wiki/public/pages/*.md'):
    content = open(f).read()
    # 提取史记引文节的引文（> > 格式）
    for q in re.findall(r'^> (.{20,})', content, re.MULTILINE):
        q_norm = re.sub(r'\s', '', q)
        if q_norm not in quotes:
            quotes[q_norm] = []
        quotes[q_norm].append(f)

for q, files in quotes.items():
    if len(files) > 1:
        print(f'重复: {q[:30]}...')
        for f in files:
            print(f'  {f}')
"
```

---

## 三、处理流程

### 情形A：同一引文出现在两页

```
判断：两页都真正需要这条引文吗？

是（两页主题都高度相关）→ 两页均保留，加交叉链接说明来源重叠
否（一页是主要讨论者，另一页是顺带提及）→ 删除次要页中的引文，保留链接
```

**操作**（以删除次要页引文为例）：

```bash
# 1. 确认哪页是主要讨论者
# 2. 在次要页中替换引文节为交叉链接
# 3. 使用 edit_page.py 修改次要页
python3 wiki/scripts/butler/edit_page.py "次要页" /tmp/updated.md \
    --summary "w10m: H12 删除重复引文，保留主要页[[主要页]]中的引文" \
    --author "butler"
```

修改后的次要页引文节示例：

```markdown
## 史记引文

（相关引文见 [[主要页]] §史记引文节）
```

### 情形B：两个概念页描述同一事物的互补方面

```
评估：是否可以合并为一个更完整的页面？

可以合并 → 判断哪页更适合作为规范页 → 进入 H1（W10a）流程
不适合整页合并（各有独特角度）→ 在两页的 ## 参见 节互链，说明互补关系
```

**互链操作**：

```bash
# 在页面A 的 ## 参见 节加入
# - [[页面B]]：从另一角度讨论同一主题
```

---

## 四、成功标准 / 完成条件

- [ ] 重复引文处理后，至少有一页保留原文，另一页改为交叉链接
- [ ] 两页互补的概念页均有 `## 参见` 节互链
- [ ] 未删除任何有价值的独有内容
- [ ] 整页重复的情形转移给 H1（W10a）处理
- [ ] diff ≤ 20 行/页

---

## 五、工具与脚本

| 工具 | 用途 |
|---|---|
| 跨页引文扫描（见上方脚本） | 发现重复引文 |
| `wiki/scripts/butler/edit_page.py` | 修改次要页 |
| `wiki/scripts/butler/record_revision.py` | 记录 revision |

---

## 六、铁律：不得删除有价值内容

```
❌ 禁止：两页都有引文，随机删掉一处
✅ 正确：确认哪页是主要讨论者后，把次要页改为交叉链接
✅ 正确：两页都有独特角度 → 互链，两份内容都保留
```

---

## 相关路径

- `wiki/logs/butler/housekeeping_queue.md` — H12 任务队列
- `skills/SKILL_W10a_Butler去重合并.md` — H1，整页重复时转入
